# Copy this file to .env.local in this scripts folder and replace with your token.
# scripts/.env.local is gitignored — never commit real tokens.
#
# GitLab → Edit profile → Access Tokens → create token with scopes: api, read_api (read_api minimum for job logs).
#
GITLAB_TOKEN=glpat_paste_your_token_here
