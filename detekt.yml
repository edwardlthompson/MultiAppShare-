build:
  maxIssues: 0
  excludeCorrectable: false

config:
  validation: true

processors:
  active: true

console-reports:
  active: true

comments:
  active: false

style:
  MaxLineLength:
    active: true
    maxLineLength: 120
  MagicNumber:
    active: false
  WildcardImport:
    active: true

complexity:
  LongMethod:
    active: true
    threshold: 80
    ignoreAnnotated:
      - Composable
  LongParameterList:
    active: true
    functionThreshold: 10
    ignoreAnnotated:
      - Composable
  CyclomaticComplexMethod:
    ignoreAnnotated:
      - Composable
  TooManyFunctions:
    active: false

naming:
  FunctionNaming:
    active: true
    ignoreAnnotated:
      - Composable

potential-bugs:
  active: true

performance:
  active: true

empty-blocks:
  active: true

coroutines:
  active: true
