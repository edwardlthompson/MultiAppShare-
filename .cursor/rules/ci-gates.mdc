---
description: Post-push CI poll protocol and gate ordering
globs:
  - ".github/workflows/**"
  - "scripts/check-github-ci.*"
  - "docs/BUILD_PLAN.md"
---

# CI Gates

## Post-Push Poll (required)

After push to `main` or before release sign-off:

```bash
bash scripts/check-github-ci.sh HEAD --wait 300
```

Primary workflow: `.github/workflows/android.yml` (`Android CI`)

## Gate Order (local — Android)

1. `bash scripts/validate-bootstrap.sh --quick`
2. `./gradlew lint test detekt koverXmlReport assembleDebug`
3. `bash scripts/feature-gate.sh --stack android`
4. `bash scripts/watch-agent-gates.sh --once --autofix --step <label>`
5. Pre-release: [`docs/PRE_RELEASE_AUDIT.md`](../../docs/PRE_RELEASE_AUDIT.md) + device smoke per [`docs/RELEASE_SMOKE.md`](../../docs/RELEASE_SMOKE.md)

**Autonomous rule:** On gate failure, agent auto-fixes from JSON output within feature scope; re-run until pass or 3-strike. Push still requires human approval.

## Do Not Mark Complete

- BUILD_PLAN `[AUTO]` items while **Android CI** / **CodeQL** red
- Sprint sign-off without `scripts/pre-release-gate.sh` on releases
